node_modules/

# Build outputs. Both are generated by `npm run build` from the sibling
# cloakllm-js checkout, so they are never edited by hand and never committed --
# a stale vendored copy of the detection engine is exactly the drift this
# extension must not have.
dist/
src/vendor/
